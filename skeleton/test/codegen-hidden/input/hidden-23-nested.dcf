class Program {
int a, b, c;
int A[10];

void func1 ( ) {
  int i,sum;
  a += 1;
  for i = 0, 10 {
    if (A[i] != 0) {
      // Breaking specs but I don't care about un-initialized arrays
      //callout("printf", "Fail");
    }
    A[i] = a + i;
  }

  for i = 0, 10 {
    sum += A[i];
  }
  
  c = sum;
}


void main ( ) {
  func1 ( );  
  callout("printf", "result should be 55: %d\n", c );
  func1 ( );  
  callout("printf",  "result should be 65: %d\n", c );
  func1 ( );  
  callout("printf", "result should be 75: %d\n", c );
}
}
