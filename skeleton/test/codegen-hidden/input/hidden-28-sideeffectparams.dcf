class Program {
int a;

int sidefx ( ) {
     a = a + 1;
     callout("printf", "side effect: a is %d\n", a);
     return a;
}

void threeargs ( int one, int two, int three ) {
     callout("printf", "three arguments. one: %d, two: %d, three: %d\n", one, two, three);
}

void main ( ) {
     a = 0;
     threeargs(sidefx(), sidefx(), sidefx());
}
}
