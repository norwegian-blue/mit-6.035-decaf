class Program {
    
    // Broken method does not return value for b == TRUE
    // Issue not spotted at semantic check
    int broken(boolean b) {
	if (!b) {
	    return 5;
	}
    }	    

    void main() {
	callout("printf", "Method should return 5: %d\n", broken(false));
	callout("printf", "Method should trigger runtime error: ");	
	callout("printf", "%d\n", broken(true));
    }
}
