class Program {
// This should produce a runtime error
int a[10];
void test (int i) {
  a[i] = 1;
  callout("printf", "%d\n", a[i] );
}
void main ( ) {
  test (-1);
  callout("printf", "Should never reach here.\n" );
}
}
